/* const return89 = () => 89;
console.log(return89()); */

/* const deviderBy17 = (number) => number / 17;
console.log(deviderBy17(100)); */

/* const operation = (first, second) => ((first + 7) + (second + 5)) % 2 == 0 ? true : false;
console.log(operation(2, 3)); */

/* const numbers = [12, 42, 52, 12, 36];

const sumSquared = (numberArray) => {
    const total = numberArray.reduce((previous, current) => previous + current, 0);
    return Math.pow(total, 2);
}

console.log(sumSquared(numbers)); */